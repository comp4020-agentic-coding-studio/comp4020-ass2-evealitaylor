---
interface WeightedMarking {
  mode: "weighted";
  criteria: { name: string; weight: number }[];
}

interface HolisticMarking {
  mode: "holistic";
  description: string;
}

interface Props {
  marking: WeightedMarking | HolisticMarking;
}

const { marking } = Astro.props;

// The table below is wrapped in .at-table-wrap by hand. The theme adds that
// scrolling wrapper to markdown tables, but not to a table written inside a
// component, and a criterion name here runs to seventy characters — which is
// how this overflowed the 390px marking viewport on three assessment pages.
---

<section aria-labelledby="how-marked">
  <h2 id="how-marked">How it is marked</h2>
  {marking.mode === "holistic" ? (
    <p>{marking.description}</p>
  ) : (
    <div class="at-table-wrap">
      <table>
        <thead><tr><th scope="col">Criterion</th><th scope="col">Weight</th></tr></thead>
        <tbody>
          {marking.criteria.map((criterion) => (
            <tr><td>{criterion.name}</td><td>{criterion.weight}%</td></tr>
          ))}
        </tbody>
      </table>
    </div>
  )}
</section>
